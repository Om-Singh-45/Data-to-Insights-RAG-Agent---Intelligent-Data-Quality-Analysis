# Copy this file to .env and replace the placeholder values with your real keys.
# DO NOT commit your real .env to source control.

# ===== REQUIRED =====
# OpenRouter key (used for LLM calls)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Google Gemini API key (used for embeddings) — this is the DEFAULT provider
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ===== OPTIONAL =====
# Embedding provider: 'gemini' (default) or 'hf' (local HuggingFace)
# If you want to use local HuggingFace embeddings instead, set this to 'hf'
EMBEDDING_PROVIDER=gemini

# Default retriever size (used when fetching source snippets)
DEFAULT_TOP_K=4
